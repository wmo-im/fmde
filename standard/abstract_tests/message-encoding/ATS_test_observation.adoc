[[ats_message-encoding-observation]]
====
[%metadata]
label:: /conf/message-encoding/observation
subject:: /req/message-encoding/observation
test-purpose:: Validate that an Observation message is valid to the FMDE schema.

[.component,class=test method]
=====
[.component,class=step]
--
Open a FirstMileMessage message / file.
--

[.component,class=step]
--
Ensure that the root field is ``data``.
--

[.component,class=step]
--
Ensure that the ``data`` field contains a valid ``observations`` field.
--

[.component,class=step]
--
For each ``observations`` field:

- ensure that one or more ``values`` fields are present
--

=====
====
