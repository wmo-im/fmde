[[req_data-model-device-ref]]
[width="90%",cols="2,6a"]
|===
^|*Requirement {counter:req-id}* |*/req/data-model/device-ref*
^|A |A DeviceRef message SHALL define an ``observerId`` element as a reference to an ObserverDevice message or a ``node`` if the ObserverDevice cannot be determined.  // TODO verify
|===
